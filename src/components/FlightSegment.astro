
---
import type { FlightSegment } from '../types';

interface Props {
  segment: FlightSegment;
  isLast?: boolean;
}

const { segment, isLast } = Astro.props;
---

<div class="segment scroll-reveal">
  <div class="time-col">
    <div class="time-block">
      <span class="time">{segment.departLocal}</span>
      <span class="location-code">{segment.from}</span>
    </div>
    
    <div class="duration-line">
       <span class="duration-text">{segment.duration}</span>
    </div>

    <div class="time-block arrival">
      <span class="time">{segment.arriveLocal}</span>
      <span class="location-code">{segment.to}</span>
    </div>
  </div>
  
  <div class="graphic-col">
    <div class="dot departing"></div>
    <div class="path-line"></div>
    <div class="dot arriving"></div>
  </div>

  <div class="info-card">
    <div class="card-header">
       <span class="airline">{segment.airline}</span>
       <span class="flight-no">{segment.flightNo}</span>
    </div>
    <div class="card-details">
       <div class="detail-row">
         <span class="label">Datum</span>
         <span class="value">{new Date(segment.date).toLocaleDateString()}</span>
       </div>
    </div>
  </div>
</div>

<style>
  .segment {
    display: grid;
    grid-template-columns: 120px 24px 1fr;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
    position: relative;
  }
  
  /* Time Column */
  .time-col {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    text-align: right;
    padding: var(--space-xs) 0;
    font-family: var(--font-mono);
  }

  .time {
    display: block;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-wilderness);
  }

  .location-code {
    display: block;
    font-size: 0.85rem;
    color: var(--color-stone);
    margin-top: 0.25rem;
  }
  
  .duration-text {
    font-size: 0.75rem;
    color: var(--color-heritage-rust);
    font-weight: 500;
  }
  
  /* Graphic Column */
  .graphic-col {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  .dot {
    width: 14px;
    height: 14px;
    border-radius: 50%;
    background-color: var(--color-paper);
    border: 3px solid var(--color-wilderness);
    z-index: 2;
  }
  
  .path-line {
    flex-grow: 1;
    width: 2px;
    background-color: var(--color-wilderness-light);
    margin: 4px 0;
    opacity: 0.3;
  }

  /* Info Card */
  .info-card {
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: var(--space-md);
    box-shadow: var(--shadow-card);
    transition: transform 0.3s ease;
  }
  
  .info-card:hover {
    transform: translateX(4px);
    border-color: var(--color-heritage-rust);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid var(--color-border);
    padding-bottom: var(--space-xs);
    margin-bottom: var(--space-sm);
  }

  .airline {
    font-family: var(--font-body);
    font-weight: 700;
    color: var(--color-steam);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .flight-no {
    font-family: var(--font-mono);
    color: var(--color-heritage-rust);
    font-size: 0.9rem;
  }

  .detail-row {
    display: flex;
    gap: var(--space-sm);
    font-size: 0.9rem;
  }
  
  .label {
    text-transform: uppercase;
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    color: var(--color-stone);
    margin-top: 2px;
  }
  
  .value {
    color: var(--color-text-main);
  }

  @media (max-width: 600px) {
    .segment {
      grid-template-columns: 60px 20px 1fr;
      gap: var(--space-sm);
    }
    .time { font-size: 1rem; }
  }
</style>
