
---
import Layout from '../layouts/Layout.astro';
import { getPath } from '../utils';

// Hardcoded list of verified images in public/images
const images = [
  "BonorongWildlifeSanctuaryTicketinBrighton.jpg",
  "Cradle Mountain Crater Falls (1).webp",
  "Cradle Mountain Dove Lake-1.webp",
  "Cradle+Mountain.webp",
  "HERO-Cradle-Mountain.jpg",
  "HastingsCaves_2018_0337-1024x768.jpg",
  "Tahune-Airwalk-Supplied-Courtesy-of-Forestry-Tasmania-129788.jpg",
  "bay_of_fires.jpg",
  "binalong.jpeg",
  "cradle-3.webp",
  "cradle-mountain2.webp",
  "david-clode-XMnQnYND9JU-unsplash.jpg",
  "donovan-simpkin-CXLTA4-TKxM-unsplash.jpg",
  "eaglehawkneck.jpg",
  "eaglehawkneck2.jpg",
  "fidel-fernando-NCnzOZDvtP0-unsplash.jpg",
  "freycinet-richardson-beach.jpg",
  "hastings-cave.webp",
  "haven-on-high-2.avif",
  "haven-on-high-2.webp",
  "haven-on-high-3.webp",
  "haven-on-high-4.avif",
  "haven-on-high.avif",
  "haven-on-high.jpeg",
  "iron-blow-lookout.jpg",
  "jordan-redshaw-V6vUX8cHjSM-unsplash.jpg",
  "launceston-airport.jpg",
  "laura-barry-HxjKuSna5Eo-unsplash.jpg",
  "matt-palmer-Hdkca4MmA58-unsplash.jpg",
  "michael-jerrard-mnHs4boXT_0-unsplash.jpg",
  "salamanca-market-book-cover.jpg",
  "schiphol-airport.jpg",
  "sean-robertson-pYDR8iOj0cE-unsplash.jpg",
  "stephen-mabbs-_DlwNohdZ4c-unsplash.jpg",
  "stingray.webp",
  "stonehouse1.avif",
  "stonehouse2.avif",
  "stonehouse3.avif",
  "stonehouse4.avif",
  "tahune-airwalks.jpg",
  "treetops1.avif",
  "treetops2.avif",
  "treetops3.jpeg",
  "treetops4.avif",
  "wineglass_bay.jpg"
].map(name => getPath(`/images/${name}`));

---

<Layout title="Gallery | Tassy Crew 2026">
  <div class="gallery-page">
    <div class="header-section text-center">
        <h1 class="page-title scroll-reveal">VISUAL EVIDENCE</h1>
        <span class="eyebrow scroll-reveal">PHOTO ARCHIVE</span>
    </div>

    <div class="gallery-grid container">
      {images.map((img, index) => (
        <div class="gallery-item scroll-reveal" onclick={`openGalleryModal(${index})`}>
          <img src={img} alt="Gallery Image" loading="lazy" />
        </div>
      ))}
    </div>
    
    <!-- Lightbox Modal -->
    <div id="lightbox" class="lightbox">
        <span class="close-btn" onclick="closeModal()">&times;</span>
        <button class="nav-btn prev-btn" onclick="changeImage(-1)" aria-label="Previous image">&#10094;</button>
        <img class="lightbox-content" id="lightbox-img" />
        <button class="nav-btn next-btn" onclick="changeImage(1)" aria-label="Next image">&#10095;</button>
    </div>
  </div>
</Layout>

<script define:vars={{ galleryImages: images }}>
  let currentIndex = 0;
  
  // Make functions available globally for inline onclick handlers
  window.openGalleryModal = function(index) {
    currentIndex = index;
    const modal = document.getElementById('lightbox');
    const modalImg = document.getElementById('lightbox-img');
    
    if (modal && modalImg) {
      modal.style.display = "flex";
      updateImage();
    }
  }

  window.closeModal = function() {
    const modal = document.getElementById('lightbox');
    if (modal) modal.style.display = "none";
  }

  window.changeImage = function(direction) {
    currentIndex += direction;
    
    // Wrap around
    if (currentIndex < 0) currentIndex = galleryImages.length - 1;
    if (currentIndex >= galleryImages.length) currentIndex = 0;
    
    updateImage();
    
    // Stop propagation if triggered from click event
    if (window.event) window.event.stopPropagation();
  }

  function updateImage() {
    const modalImg = document.getElementById('lightbox-img');
    if (modalImg && galleryImages[currentIndex]) {
      modalImg.src = galleryImages[currentIndex];
    }
  }

  // Close on outside click and keyboard nav
  document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('lightbox');
    
    if (modal) {
      modal.addEventListener('click', (e) => {
        if (e.target === modal) {
          window.closeModal();
        }
      });
    }

    document.addEventListener('keydown', (e) => {
      const modal = document.getElementById('lightbox');
      if (modal && modal.style.display === 'flex') {
        if (e.key === 'ArrowLeft') window.changeImage(-1);
        if (e.key === 'ArrowRight') window.changeImage(1);
        if (e.key === 'Escape') window.closeModal();
      }
    });
  });
</script>

<style>
  .nav-btn {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(0,0,0,0.5);
    border: none;
    color: #fff;
    font-size: 2rem;
    padding: 1rem;
    cursor: pointer;
    transition: background 0.3s, color 0.3s;
    user-select: none;
    z-index: 2001;
  }

  .nav-btn:hover {
    background: rgba(212, 160, 61, 0.5);
    color: var(--color-accent-gold);
  }

  .prev-btn {
    left: 20px;
  }

  .next-btn {
    right: 20px;
  }

  @media (max-width: 768px) {
    .nav-btn {
      padding: 0.5rem;
      font-size: 1.5rem;
    }
  }

  .gallery-page {
    background-color: var(--color-bg-dark);
    min-height: 100vh;
    padding-bottom: var(--space-xl);
  }

  .header-section {
    padding: var(--space-lg) var(--space-md);
    margin-bottom: var(--space-lg);
    border-bottom: 2px solid var(--color-accent-gold);
  }

  .page-title {
    font-size: clamp(3rem, 6vw, 6rem);
    color: var(--color-text-cream);
    margin: 0;
    line-height: 0.9;
  }
  
  .eyebrow {
    font-family: var(--font-mono);
    color: var(--color-accent-gold);
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: var(--space-sm);
    padding: 0 var(--space-md);
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    aspect-ratio: 1; /* Square tiles */
    border-radius: 4px;
    cursor: pointer;
    border: 1px solid rgba(255,255,255,0.1);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .gallery-item:hover {
    transform: scale(1.02);
    border-color: var(--color-accent-gold);
    z-index: 2;
  }

  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: filter 0.3s ease;
    filter: sepia(0.2) contrast(1.1);
  }
  
  .gallery-item:hover img {
    filter: none;
  }

  /* Lightbox */
  .lightbox {
    display: none;
    position: fixed;
    z-index: 1000;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.95);
    justify-content: center;
    align-items: center;
    padding: 2rem;
  }

  .lightbox-content {
    max-width: 90%;
    max-height: 90vh;
    border: 2px solid var(--color-accent-gold);
    box-shadow: 0 0 20px rgba(0,0,0,0.5);
  }

  .close-btn {
    position: absolute;
    top: 20px;
    right: 40px;
    color: #f1f1f1;
    font-size: 40px;
    font-weight: bold;
    cursor: pointer;
  }
  
  .close-btn:hover {
    color: var(--color-accent-gold);
  }
</style>
